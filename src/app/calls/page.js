import CallList from 'src/sections/calls/view/index'

// ----------------------------------------------------------------------

export const metadata = {
  title: 'Calls',
};

export default function FileManagerPage() {
  return <CallList />;
}
