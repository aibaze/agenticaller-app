import PhoneNumberList from 'src/sections/phone-numbers/view';

// ----------------------------------------------------------------------

export const metadata = {
  title: 'Phone Numbers',
};

export default function PhoneNumbersPage() {
  return <PhoneNumberList />;
} 