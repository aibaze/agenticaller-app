import { BetaView } from 'src/sections/beta/view';

// ----------------------------------------------------------------------

export const metadata = {
  title: 'Allwyse Beta',
};

export default function BetaViewPage({ params }) {
  return <BetaView />;
}
