import UserSettings from 'src/sections/user-settings/view';

// ----------------------------------------------------------------------

export const metadata = {
  title: 'User: Setting',
};

export default function Page() {
  return <UserSettings />;
}
