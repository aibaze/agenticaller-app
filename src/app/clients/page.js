import { ClientList } from 'src/sections/clients/view';

// ----------------------------------------------------------------------

export const metadata = {
  title: 'Clients',
};

export default function FileManagerPage() {
  return <ClientList />;
}
