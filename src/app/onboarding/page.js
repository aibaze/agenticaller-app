import OnboardingView from 'src/sections/onboarding/OnboardingView';

// ----------------------------------------------------------------------

export const metadata = {
  title: 'Coach onboarding',
};

export default function LoginPage() {
  return <OnboardingView />;
}
