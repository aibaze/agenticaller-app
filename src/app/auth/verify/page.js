import { VerfifyView } from 'src/sections/auth/jwt';

// ----------------------------------------------------------------------

export const metadata = {
  title: 'Verify',
};

export default function RegisterPage() {
  return <VerfifyView />;
}
